from abc import ABC, abstractmethod


class Tools(ABC):
    # Por hora não vou aplicar esses separadores, pois isso limitaria a pesquisa de quem fosse utilizar a biblioteca, melhor deixar documentado quais as formas de pesquisa na documentação
    # @abstractmethod
    # def separating_with_and(self, text:str):
    #     keywords = text.split()
    #     return " and ".join(keywords)
    

    
    # @abstractmethod
    # def separating_with_or(self, text:str):
    #     keywords = text.split()
    #     return " and ".join(keywords)
    
    
    # @abstractmethod
    # def using_wild_card(self, text:str):
    #     keywords = text.split()
    #     return " and ".join(keywords)
    pass